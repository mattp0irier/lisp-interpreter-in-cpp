(define getMax (a) (begin
    (set max (car a))
    (set list a)
    (while (notnull? (cdr list)) (begin
        (if (> (car list) max) (set max (car list)) ())
        (set list (cdr list))
    ))
    (if (> (car list) max) (set max (car list)) ())
    max
))

(define reverse (a)

)

(print "what")

(set b (1 2))
(print b)
(set b (remove b 2))
(print b)
(set a (1 2 5 4 5 2))
(getMax a)
